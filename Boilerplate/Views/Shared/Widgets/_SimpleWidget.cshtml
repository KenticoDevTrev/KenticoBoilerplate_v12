@using Models.InlineEditors
@using Models.Widgets
@model Models.Widgets.SimpleWidgetViewModel

@if (Context.Kentico().PageBuilder().EditMode)
{
    // Assign any Inline Editors here
    Html.RenderPartial("InlineEditors/_TinyMCEEditor", new TinyMCEEditorViewModel
    {
        PropertyName = nameof(SimpleWidgetProperties.Html),
        Html = Model.Html
    });
}
else
{
    @Html.Raw(Model.Html);
}
